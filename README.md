# Fortune AI

Android application that integrates with XAI and ElevenLabs APIs.

## Building and Releasing
A GitHub Actions workflow is set up to automatically build a signed release APK when changes are pushed to `main` or `master`.
